<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lchrislee.worldplanner">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".WorldPlannerApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/WorldPlannerTheme">
        <activity android:name=".activities.WorldPlannerBaseActivity" />
        <activity android:name=".activities.CurrentWorldActivity"
            android:windowSoftInputMode="adjustPan"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.EntityDetailActivity"
            android:parentActivityName=".activities.CurrentWorldActivity"
            android:windowSoftInputMode="adjustPan"
            >
        </activity>

        <activity
            android:name=".activities.RelationDetailActivity"
            android:parentActivityName=".activities.CurrentWorldActivity"
            android:windowSoftInputMode="adjustPan"
            />

        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="c30a7a54a2ff15981fe537e9bbe190f123393b76" />

        <meta-data android:name="DATABASE" android:value="world_planner_sugar.db" />
        <meta-data android:name="VERSION" android:value="1" />
        <meta-data android:name="DOMAIN_PACKAGE_NAME" android:value="com.lchrislee.worldplanner"/>

    </application>

</manifest>